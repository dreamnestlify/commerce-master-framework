{
    "name": "commerce-master/commerce-block-theme",
    "version": "0.1.0",
    "description": "Gutenberg FSE block theme for fashion e-commerce master template",
    "private": true,
    "scripts": {
        "lint": "npm run lint:js && npm run lint:css",
        "lint:js": "eslint assets/js/ --ext .js",
        "lint:css": "stylelint \"assets/css/**/*.css\"",
        "lint:fix": "eslint assets/js/ --ext .js --fix && stylelint \"assets/css/**/*.css\" --fix",
        "build": "echo 'Theme uses native Gutenberg blocks — no build step required'",
        "format": "prettier --write \"**/*.{js,css,json}\""
    },
    "devDependencies": {
        "eslint": "^9.0.0",
        "stylelint": "^16.0.0",
        "stylelint-config-standard": "^36.0.0",
        "prettier": "^3.0.0"
    }
}
